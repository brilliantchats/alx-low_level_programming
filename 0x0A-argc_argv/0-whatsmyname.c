#include "main.h"
#include <stdio.h>
/**
 * main - Entry point
 * @argc: counter of arguments
 * @argv: array of parameters
 *
 * Return: 0(Success)
 */
int main(int argc, char *argv[])
{
	(void)argc;

	printf("%s", argv[0]);
	return (0);
}
