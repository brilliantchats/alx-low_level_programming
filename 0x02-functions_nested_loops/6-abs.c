#include "main.h"

/**
 * _abs - Print absolute number
 * @x: number passed
 *
 * Description: Output the absolute number from x
 *
 * Return: absolute number(Success)
 *
 */
int _abs(int x)
{
	if (x >= 0)
	{
		return (x);
	} else if (x < 0)
	{
		return (x * -1)
	}
}
